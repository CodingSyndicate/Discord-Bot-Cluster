/**
 * Copy from here!
 */

const { MessageReaction, User, Message } = require("discord.js");

module.exports = {
    message,
    voiceStateUpdate
}

/**
 * Emitted whenever a chennel is created.
 * @param {DMChannel}
 */
function channelCreate (channel) {

}



/**
 * message Event Handler
 * @param {Message} message The received message
 */
function message(message) {

}

/**
 * voiceStateUpdate Event Handler
 * @param {VoiceState} oldVoiceState the old voice state of the user
 * @param {VoiceState} newVoiceState the new voice state of the user
 */
function voiceStateUpdate(oldVoiceState, newVoiceState) {

}

